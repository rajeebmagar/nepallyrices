import { GenreToTagInputOptionPipe } from './genre-to-tag-input-option.pipe';

describe('GenreToTagInputOptionPipe', () => {
  it('create an instance', () => {
    const pipe = new GenreToTagInputOptionPipe();
    expect(pipe).toBeTruthy();
  });
});
