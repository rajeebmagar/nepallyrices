import { UserIntroToIntroViewContentPipe } from './user-intro-to-intro-view-content.pipe';

describe('UserIntroToIntroViewContentPipe', () => {
  it('create an instance', () => {
    const pipe = new UserIntroToIntroViewContentPipe();
    expect(pipe).toBeTruthy();
  });
});
