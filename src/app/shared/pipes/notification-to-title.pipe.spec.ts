import { NotificationToTitlePipe } from './notification-to-title.pipe';

describe('NotificationToTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new NotificationToTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
