import { NotificationToUrlPipe } from './notification-to-url.pipe';

describe('NotificationToUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new NotificationToUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
