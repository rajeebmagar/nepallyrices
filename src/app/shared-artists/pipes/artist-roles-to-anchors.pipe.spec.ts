/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ArtistRolesToAnchorsPipe } from './artist-roles-to-anchors.pipe';

describe('artistRolesToAnchorsPipe', () => {
  it('create an instance', () => {
    const pipe = new ArtistRolesToAnchorsPipe();
    expect(pipe).toBeTruthy();
  });
});
