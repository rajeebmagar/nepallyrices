import { ArtistRoleToTagInputOptionPipe } from './artist-role-to-tag-input-option.pipe';

describe('ArtistRoleToTagInputOptionPipe', () => {
  it('create an instance', () => {
    const pipe = new ArtistRoleToTagInputOptionPipe();
    expect(pipe).toBeTruthy();
  });
});
