import { GenresToAnchorsPipe } from './genres-to-anchors.pipe';

describe('GenresToAnchorsPipe', () => {
  it('create an instance', () => {
    const pipe = new GenresToAnchorsPipe();
    expect(pipe).toBeTruthy();
  });
});
