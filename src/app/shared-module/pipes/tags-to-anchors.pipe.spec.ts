import { TagsToAnchorsPipe } from './tags-to-anchors.pipe';

describe('TagsToAnchorsPipe', () => {
  it('create an instance', () => {
    const pipe = new TagsToAnchorsPipe();
    expect(pipe).toBeTruthy();
  });
});
