import { TagToTagInputOptionPipe } from './tag-to-tag-input-option.pipe';

describe('TagToTagInputOptionPipe', () => {
  it('create an instance', () => {
    const pipe = new TagToTagInputOptionPipe();
    expect(pipe).toBeTruthy();
  });
});
