import { ImageDefaultUrlPipe } from './image-default-url.pipe';

describe('ImageDefaultUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageDefaultUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
